import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fadedskies.driverapp',
  appName: 'Faded Skies Driver App',
  webDir: 'dist'
};

export default config;
